function [x_m, f_m] = Fibonacci(a, b, n, f)
c = a + (fibonacci(n)/fibonacci(n+2))*(b-a);
d = a + (fibonacci(n+1)/fibonacci(n+2))*(b-a);
while n>0
    if (f(c)<=f(d))
        b=d;
        d=c;
        c=a+b-d;
    else
        a=c;
        c=d;
        d=a+b-c;
    end
n=n-1;
end
end